name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main
    paths:
      # everything Firebase serves — html, app-core.js, firebase-config.js and
      # anything added later. Pushes that only touch README or the workflows
      # themselves still skip the deploy.
      - 'public/**'
      # firebase.json controls what `firebase deploy --only hosting` actually
      # does (headers, ignore list, public dir) even though it isn't served
      # itself - a config-only change here needs to deploy too. Missing this
      # is exactly what silently stranded the cache-header fix after PR #21.
      - 'firebase.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Deploy to Firebase Hosting
        run: firebase deploy --only hosting --project my-app-hub-69db8 --token "$FIREBASE_TOKEN"
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
